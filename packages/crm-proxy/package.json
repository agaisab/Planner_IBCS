{
  "name": "crm-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "proxy service that handles NTLM-authenticated requests to the CRM",
  "scripts": {
    "dev": "NODE_ENV=development node --watch src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "httpntlm": "^1.8.11",
    "morgan": "^1.10.0"
  }
}
